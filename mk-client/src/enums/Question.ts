export enum CompetitorComparison {
    OUTMATCH = "outmatch",
    FALLBEHIDE = "fall-behind",
    EVEN = "even",
    NOSPEC = "no-spec",
}

export enum FinalDecision {
    USETOA = "use-toa",
    NOTUSETOA = "not-use-toa",
    TENTATIVE = "tentative",
}
